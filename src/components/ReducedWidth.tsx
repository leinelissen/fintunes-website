import styled from 'styled-components';
import VerticalSpacing from './VerticalSpacing';

const ReducedWidth = styled(VerticalSpacing)`
    max-width: 640px;
    margin: 0 auto;
    text-align: center;
`;

export default ReducedWidth;
